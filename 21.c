#include<stdio.h>

int main()
{
    char str[20] = "ranjeet";
    int len = 0;
    //for(int i = 0;str[i]!='\0';++i)
    while(str[len]!='\0')
    {
        len ++;
       // printf("\n length of the string are %d",len);
    }
  
    printf("\n length of the string are %d",len);
        //return 0;
  
   
return 0;

}